<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notificationBackground"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/notification_background"
    android:elevation="20dp">

    <!-- Header with Category Title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <!-- Category Title -->
        <TextView
            android:id="@+id/categoryTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Content Detected"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:gravity="center_vertical" />

        <!-- Close Button -->
        <ImageButton
            android:id="@+id/closeButton"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_close"
            android:background="@android:color/transparent"
            android:tint="@android:color/white"
            android:alpha="0.8" />

    </LinearLayout>

    <!-- Why we're deleting -->
    <TextView
        android:id="@+id/deleteReason"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="This content violates our safety guidelines"
        android:textSize="16sp"
        android:textColor="@android:color/white"
        android:alpha="0.9"
        android:layout_marginBottom="12dp"
        android:gravity="center_vertical" />

    <!-- What the user typed -->
    <TextView
        android:id="@+id/deletedContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You typed: Content preview will appear here"
        android:textSize="16sp"
        android:textColor="@android:color/white"
        android:alpha="0.95"
        android:padding="16dp"
        android:layout_marginBottom="16dp"
        android:gravity="start"
        android:maxLines="4"
        android:ellipsize="end"
        android:layout_marginTop="8dp" />

    <!-- Spacer to push buttons to bottom -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Action Buttons Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- Undo Button -->
        <Button
            android:id="@+id/undoButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="UNDO"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:background="@drawable/undo_button_background"
            android:layout_marginEnd="8dp" />

        <!-- Dismiss Button -->
        <Button
            android:id="@+id/dismissButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="DISMISS"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:background="@drawable/dismiss_button_background"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <!-- Progress Bar for Auto-hide -->
    <ProgressBar
        android:id="@+id/autoHideProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginTop="16dp"
        android:progressTint="@android:color/white"
        android:progressBackgroundTint="@android:color/white"
        android:progressBackgroundTintMode="multiply"
        android:alpha="0.4"
        android:max="100"
        android:progress="100" />

</LinearLayout>
